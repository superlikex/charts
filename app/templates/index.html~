{% extends "base.html" %}
{% import "bootstrap/wtf.html" as wtf %}

{% block title %}ballot{% endblock %}

{% block page_content %}
<div class="page-header">
	<h1>抽吧</h1>
</div>
{{ wtf.quick_form(form) }}
</br>
</br>
	<h2>历史记录：</h2>
	{% for post in history %}
	{% if post.result %}
	<ul class="post">
		<p>{{ post.username }} 课题{{ post.result%2+1 }} {{ moment(post.ballot_time).format('LLL')}}.</p>
			
	</ul>
	{% endif %}
	{% endfor %}

	<h2>未抽小组：</h2>
	{% for post in history %}
	{% if not post.result %}
	<ul class="post">
		<p>{{post.username}}</p>
	</ul>
	{% endif %}	
	{% endfor %}
{% endblock %}
